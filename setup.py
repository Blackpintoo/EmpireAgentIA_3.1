from setuptools import setup, find_packages

setup(
    name="empire_agent_ia",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],  # ajoute ici tes dépendances éventuelles
)
