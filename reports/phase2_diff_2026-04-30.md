# Rapport Phase 2 — diff complet et validation

- **Date** : 2026-04-30
- **Tags de sauvegarde créés** :
  - `pre-phase2-2026-04-30` (avant TÂCHE 1/2)
  - `pre-fix-pointvalue-2026-04-30` (avant TÂCHE 0)
  - les deux pointent sur le même HEAD `6370cdb`
- **Aucun commit, aucun push.** Tous les changements sont dans le working tree.

---

## 1. Test voie native MT5 — UK100 / GER40 (préalable TÂCHE 0)

Lecture seule via `mt5.symbol_info()` le 2026-04-30, compte démo Vantage `11535481` (USD).

| Symbole | currency_profit | trade_tick_value | trade_tick_size | **point_val natif (tv/ts)** | Override actuel | Écart |
|---|---|---:|---:|---:|---:|---:|
| **UK100** | GBP | 0.01359 | 0.01 | **1.3593 USD/pt** | 1.0 USD/pt | **-26,5 %** |
| **GER40** | EUR | 0.01173 | 0.01 | **1.1731 USD/pt** | 1.0 USD/pt | **-14,8 %** |
| DJ30 | USD | 0.01 | 0.01 | 1.0000 USD/pt | 1.0 USD/pt | OK |
| USDCAD | CAD | 0.7353 | 1e-05 | 73 528 USD/pt | (pas dans dict) | OK |
| GBPUSD | USD | 1.0 | 1e-05 | 100 000 USD/pt | (pas dans dict) | OK |
| XAGUSD | USD | 5.0 | 0.001 | 5 000 USD/pt | (pas dans dict) | OK |

**Verdict** : la voie native MT5 fonctionne et confirme l'écart structurel. L'override hardcodé `_indices_point_val` à 1.0 sous-estimait le risque de 26,5 % pour UK100 et 14,8 % pour GER40. Ces valeurs sont en ligne avec les taux GBPUSD/EURUSD courants (~1,36 / ~1,17). DJ30, USDCAD, GBPUSD, XAGUSD sont correctement traités sans override.

**Audit logique parallèle** (TÂCHE préliminaire) : grep sur `point_val | _indices_point_val | tick_value | risk_per_trade`. La logique d'override existe **uniquement** à `orchestrator/orchestrator.py:2950`. `backtest/agent_backtest.py`, `utils/risk_sizing.py`, `utils/risk_manager.py` utilisent des chemins différents et ne reproduisent pas le bug.

---

## 2. Diff `orchestrator/orchestrator.py`

Deux modifications dans le même diff (TÂCHE 0 + TÂCHE 2). Le diff complet :

```diff
diff --git a/orchestrator/orchestrator.py b/orchestrator/orchestrator.py
index b96532b..089d0a7 100644
--- a/orchestrator/orchestrator.py
+++ b/orchestrator/orchestrator.py
@@ -59,6 +59,11 @@ from reporting.daily_digest import send_daily_digest
 from utils.live_metrics import should_allow_live, rolling_metrics
 from utils.audit import append as audit_append
 from utils.health import start_health_server
+
+# FIX 2026-04-30: Symboles autorisés à contourner la blacklist horaire globale
+# via leur whitelist locale (allowed_hours_utc). Pour tous les autres symboles,
+# la blacklist globale s'applique strictement (union global ∪ local).
+BLACKLIST_OVERRIDE_WHITELIST = ["XAUUSD"]
 from agents.whale_agent import WhaleAgent
```

### TÂCHE 2 — Sémantique blacklist horaire stricte

```diff
@@ -2250,8 +2255,45 @@ class Orchestrator:
         current_hour_utc = datetime.now(timezone.utc).hour
         orch_cfg = (self.profile.get("orchestrator") or {})

-        blocked_hours = orch_cfg.get("blocked_hours_utc", [])
+        # FIX 2026-04-19 D4: Union blacklist globale + locale.
+        # FIX 2026-04-30: Sémantique stricte. Seuls les symboles listés dans
+        # BLACKLIST_OVERRIDE_WHITELIST peuvent contourner la blacklist globale via
+        # leur allowed_hours_utc local (cas XAUUSD documenté). Pour tous les autres
+        # symboles, la blacklist globale s'applique inconditionnellement.
+        local_blocked = list(orch_cfg.get("blocked_hours_utc", []) or [])
         allowed_hours = orch_cfg.get("allowed_hours_utc", None)
+        _global_blocked: list = []
+        try:
+            from utils.config import get_overrides as _get_overrides
+            _ov = _get_overrides() or {}
+            _global_blocked = list(
+                ((_ov.get("GLOBAL") or {}).get("orchestrator") or {}).get("blocked_hours_utc", []) or []
+            )
+        except Exception:
+            _global_blocked = []
+
+        _sym_upper = (symbol or "").upper()
+        if _sym_upper in BLACKLIST_OVERRIDE_WHITELIST:
+            # Exception nommée: la whitelist locale a priorité sur la blacklist globale
+            _allowed_set = set(allowed_hours or [])
+            blocked_hours = sorted(
+                (set(local_blocked) | set(_global_blocked)) - _allowed_set
+            )
+            # Log des heures où l'exception s'applique
+            _bypass_now = (
+                current_hour_utc in _global_blocked
+                and current_hour_utc in _allowed_set
+                and current_hour_utc not in blocked_hours
+            )
+            if _bypass_now:
+                logger.info(
+                    f"[HOUR_FILTER][EXCEPTION] {symbol} autorisé sur h{current_hour_utc} "
+                    f"via allowed_hours_utc local malgré blacklist globale"
+                )
+        else:
+            # Mode strict: union global ∪ local, sans soustraire allowed_local
+            blocked_hours = sorted(set(local_blocked) | set(_global_blocked))
```

### TÂCHE 0 — Fix `_indices_point_val`

```diff
@@ -2929,23 +2971,31 @@ class Orchestrator:
                 except Exception as _pv_err:
                     logger.warning(f"[RISK_CAP] symbol_info({broker_symbol}) échoué: {_pv_err}")

-                # FIX 2026-03-23 R15: Override point_val pour indices CFD
-                # contract_size × point donne 0.01 pour SP500/NAS100
-                # mais le risque réel est ~$1/pt/lot (vérifié empiriquement)
+                # FIX 2026-03-23 R15: Override point_val pour indices CFD libellés en USD.
+                # contract_size × point donne 0.01 pour SP500/NAS100 mais le risque réel
+                # est ~$1/pt/lot (vérifié empiriquement).
+                # FIX 2026-04-30: retrait UK100/GER40 du dict point_value override.
+                # Ces indices ont currency_profit GBP/EUR; l'override 1.0 sous-estimait
+                # le risque (~36% UK100, ~17% GER40 selon mt5.symbol_info live).
+                # Ils utilisent désormais la voie native MT5 (trade_tick_value/trade_tick_size).
+                # L'override n'est conservé que pour les indices avec currency_profit == USD.
                 _indices_point_val = {
                     "SP500": 1.0, "SP500#1": 1.0,
                     "NAS100": 1.0, "NAS100#1": 1.0,
                     "DJ30": 1.0, "DJ30#1": 1.0,
-                    "GER40": 1.0, "UK100": 1.0,
                 }
                 _sym_upper = symbol.upper()
                 if _sym_upper in _indices_point_val:
                     _old_pv = _point_val
                     _point_val = _indices_point_val[_sym_upper]
                     logger.info(
-                        f"[RISK_CAP] {symbol}: point_val override indices = {_point_val} "
+                        f"[RISK_CAP] {symbol}: mode=override_indices_USD point_val={_point_val} "
                         f"(était {_old_pv})"
                     )
+                else:
+                    logger.info(
+                        f"[RISK_CAP] {symbol}: mode=MT5_native_tick_conversion point_val={_point_val:.4f}"
+                    )
```

`py_compile` du fichier passe. La constante `BLACKLIST_OVERRIDE_WHITELIST` est définie au niveau module entre deux blocs d'imports — Python l'autorise et la constante est accessible depuis la méthode où elle est utilisée.

---

## 3. Diff `config/overrides.yaml`

400 lignes de diff. Sections principales :

### 3.1 BNBUSD — bascule shadow

```diff
@@ -265,7 +271,7 @@ LTCUSD:
 BNBUSD:
   orchestrator:
-    auto_execute: true
+    auto_execute: false                     # SHADOW 2026-04-30: bascule shadow mode (était true)
     telegram_validation: false
```

Aucun autre paramètre BNBUSD modifié.

### 3.2 SOLUSD — auto_execute explicite

```diff
@@ -567,30 +577,33 @@ EURUSD:
 SOLUSD:
   orchestrator:
+    auto_execute: false                   # SHADOW 2026-04-30: bascule shadow mode explicite
+    telegram_validation: false
     # R18: MODE PROBATION
     probation: true
```

### 3.3 UK100 — passage shadow + paramètres normalisés

```diff
@@ -671,19 +703,22 @@ GBPUSD:
-# UK100 — Indice UK
+# UK100 — Indice UK (SHADOW 2026-04-30: hérite NAS100 base, currency_profit=GBP)
+# point_value géré désormais via voie native MT5 (Tâche 0 30/04 — orchestrator.py)
 UK100:
   orchestrator:
+    auto_execute: false                   # SHADOW 2026-04-30
+    telegram_validation: false
     whale_override:
       enable: false
     position_limits:
-      max_volume: 2.0
+      max_volume: 0.5                     # SHADOW 2026-04-30 (volume_step broker = 0.1)
     cooldown:
       enabled: true
-      min_secs_between_trades: 600
-      max_trades_per_day: 1
+      min_secs_between_trades: 900        # SHADOW 2026-04-30
+      max_trades_per_day: 3               # SHADOW 2026-04-30
```

### 3.4 GBPUSD — passage shadow

```diff
-# GBPUSD — Forex
+# GBPUSD — Forex (SHADOW 2026-04-30: hérite EURUSD)
 GBPUSD:
   orchestrator:
+    auto_execute: false                   # SHADOW 2026-04-30
+    telegram_validation: false
     position_limits:
-      max_volume: 2.0
+      max_volume: 1.0                     # SHADOW 2026-04-30 (forex)
     cooldown:
       enabled: true
-      min_secs_between_trades: 600
-      max_trades_per_day: 1
+      min_secs_between_trades: 900        # SHADOW 2026-04-30
+      max_trades_per_day: 3               # SHADOW 2026-04-30
```

### 3.5 Nouvelles sections : DJ30, USDCAD, GER40, XAGUSD

```yaml
# DJ30 — Indice US Dow Jones (SHADOW 2026-04-30: hérite NAS100 base, USD natif)
DJ30:
  orchestrator:
    auto_execute: false                   # SHADOW 2026-04-30
    telegram_validation: false
    whale_override: {enable: false}
    position_limits:
      max_volume: 0.5                     # SHADOW 2026-04-30 (volume_step broker = 0.1)
    cooldown:
      enabled: true
      min_secs_between_trades: 900
      max_trades_per_day: 3
      after_loss_min: 45
      after_streak_n: 3
      after_streak_min: 30
    atr_sl_mult: 1.6                      # Hérité NAS100
    atr_tp_mult: 2.5
    prime_hours_utc:
      - {start: 13, end: 20}              # Hérité NAS100 (session NY cash)
    position_manager:
      enabled: true
      max_duration_minutes: 300
  risk:
    risk_per_trade: 0.005                 # SHADOW conservateur
    daily_loss_abs: 200
    max_consec_losses: 3

# USDCAD — Forex (SHADOW 2026-04-30: hérite EURUSD; corrélation pétrole/EIA 14h-16h UTC)
# Note: currency_profit=CAD, conversion native MT5 OK (vérifié 30/04 mt5.symbol_info)
USDCAD:
  orchestrator:
    auto_execute: false
    telegram_validation: false
    whale_override: {enable: false}
    position_limits:
      max_volume: 1.0                     # SHADOW 2026-04-30 (forex)
    cooldown:
      enabled: true
      min_secs_between_trades: 900
      max_trades_per_day: 3
      after_loss_min: 60
      after_streak_n: 3
      after_streak_min: 30
    atr_sl_mult: 1.4                      # Hérité EURUSD
    atr_tp_mult: 2.5
    position_manager:
      max_duration_minutes: 360
  risk:
    risk_per_trade: 0.005
    daily_loss_abs: 100

# GER40 — Indice DAX (SHADOW 2026-04-30: hérite NAS100, EUR, ATR_SL +20%, filling IOC)
# point_value géré désormais via voie native MT5 (Tâche 0 30/04 — orchestrator.py)
GER40:
  orchestrator:
    auto_execute: false
    telegram_validation: false
    whale_override: {enable: false}
    position_limits:
      max_volume: 0.5
    cooldown:
      enabled: true
      min_secs_between_trades: 900
      max_trades_per_day: 3
      after_loss_min: 45
      after_streak_n: 3
      after_streak_min: 30
    atr_sl_mult: 1.92                     # NAS100 1.6 × +20% (volatilité DAX)
    atr_tp_mult: 2.5
    prime_hours_utc:
      - {start: 7, end: 16}
    position_manager:
      enabled: true
      max_duration_minutes: 300
    filling_type: "IOC"                   # prévention retcode 10030
  risk:
    risk_per_trade: 0.005
    daily_loss_abs: 200
    max_consec_losses: 3

# XAGUSD — Argent (SHADOW 2026-04-30: hérite XAUUSD, ATR_SL +30%, filling IOC)
XAGUSD:
  orchestrator:
    auto_execute: false
    telegram_validation: false
    whale_override: {enable: false}
    position_limits:
      max_volume: 1.0                     # volume_max broker = 20
    cooldown:
      enabled: true
      min_secs_between_trades: 900
      max_trades_per_day: 3
      after_loss_min: 60
      after_streak_n: 2
      after_streak_min: 60
    atr_sl_mult: 2.08                     # XAUUSD 1.6 × +30%
    atr_tp_mult: 2.5
    prime_hours_utc:
      - {start: 7, end: 17}
    position_manager:
      enabled: true
      max_duration_minutes: 360
    filling_type: "IOC"
  risk:
    risk_per_trade: 0.005
    daily_loss_abs: 100
    max_consec_losses: 2
```

### 3.6 État final des paramètres clés

| Symbole | auto_execute | max_volume | max_trades/day | cooldown (s) | atr_sl_mult | filling |
|---|---|---:|---:|---:|---:|---:|
| DJ30 | false | 0.5 | 3 | 900 | 1.6 | (broker IOC natif) |
| UK100 | false | 0.5 | 3 | 900 | 1.6 | (broker IOC natif) |
| GBPUSD | false | 1.0 | 3 | 900 | 1.4 | (broker IOC natif) |
| USDCAD | false | 1.0 | 3 | 900 | 1.4 | (broker IOC natif) |
| GER40 | false | 0.5 | 3 | 900 | **1.92** (+20 %) | **IOC explicite** |
| XAGUSD | false | 1.0 | 3 | 900 | **2.08** (+30 %) | **IOC explicite** |
| BNBUSD | **false** (était true) | 1.0 | 1 (inchangé) | 600 (inchangé) | — | — |
| SOLUSD | **false** (explicite) | 5.0 (inchangé) | 1 (inchangé) | 600 (inchangé) | — | — |

YAML parse OK : `python -c "import yaml; yaml.safe_load(open('config/overrides.yaml'))"` sans erreur.

---

## 4. Diff `config/profiles.yaml`

213 lignes de diff. Sections principales :

### 4.1 enabled_symbols étendu

```diff
@@ -34,10 +34,22 @@ enabled_symbols:
 # CRYPTOS (4) - Avec filtres heures BLACKLIST/WHITELIST
-- BNBUSD        # Binance Coin - BLACKLIST heures [1,4] UTC
+- BNBUSD        # SHADOW 2026-04-30 (auto_execute=false dans overrides.yaml)
 - LTCUSD
 - BTCUSD
-- SOLUSD        # Réactivé 2026-02-11
+- SOLUSD        # SHADOW 2026-04-30 (auto_execute=false dans overrides.yaml)
+
+# CANDIDATS PHASE 2 SHADOW MODE (ajoutés 2026-04-30)
+- DJ30          # Indice US (USD), hérite NAS100
+- UK100         # Indice UK (GBP), point_value via voie native MT5
+- GBPUSD        # Forex, hérite EURUSD
+- USDCAD        # Forex (CAD natif), hérite EURUSD
+- GER40         # Indice DAX (EUR), filling IOC, ATR_SL +20%
+- XAGUSD        # Argent (USD), hérite XAUUSD, filling IOC, ATR_SL +30%
```

Et nettoyage de la section "DESACTIVES" (suppression des entrées GBPUSD/XAGUSD/UK100 qui sont désormais en shadow).

### 4.2 Profils existants — passage shadow

GBPUSD, UK100, XAGUSD ont leur clé `enabled` passée à `true`, leur `auto_execute` à `false`, et leur `tier` passé à `phase2_shadow` avec `risk_per_trade: 0.005`. Pour UK100, le `pip_value` indicatif est passé de 0.01 à 1.36 (la voie native MT5 reste utilisée à l'exécution).

### 4.3 Nouveaux profils

DJ30, USDCAD, GER40 ajoutés avec structure minimale (symbol, enabled, instrument, risk, orchestrator.auto_execute=false, agents). La structure suit exactement celle de UK100/XAGUSD existants.

### 4.4 État final dans `profiles.yaml`

| Symbole | enabled (profile) | auto_execute (profile) | auto_execute (override) | dans enabled_symbols |
|---|---|---|---|---|
| DJ30 | true | false | false | ✅ |
| UK100 | true | false | false | ✅ |
| GBPUSD | true | false | false | ✅ |
| USDCAD | true | false | false | ✅ |
| GER40 | true | false | false | ✅ |
| XAGUSD | true | false | false | ✅ |
| BNBUSD | true | true (non modifié) | **false** (override prime) | ✅ |
| SOLUSD | true | true (non modifié) | **false** (override prime) | ✅ |

Pour BNBUSD/SOLUSD, le brief demandait modifier seulement `overrides.yaml`. La logique de merge `utils/config.deep_update` fait que la valeur d'`overrides.yaml` écrase celle de `profiles.yaml`. Le shadow mode est donc effectif pour ces deux symboles via l'override. À noter pour cohérence future si un audit visuel est mené sur `profiles.yaml` seul.

---

## 5. Diagnostic AssetManager — verdict synthétique

| Question | Réponse |
|---|---|
| Existe-t-il une logique de conversion devise dans `utils/asset_manager.py` ? | **Non.** Aucune mention de currency / convert / exchange. La conversion est implicite via MT5. |
| Existe-t-il une logique de conversion devise dans `utils/risk_manager.py` ? | **Non.** Le module utilise `pip_value` lu depuis le profil (`profiles.yaml`) ou `contract_size × point` en fallback. Pas de conversion vers la devise du compte. |
| La conversion est-elle gérée ailleurs ? | **Oui, partiellement, dans `orchestrator.py:2920-2980`**. Pour les symboles non-indices, la voie native MT5 (`trade_tick_value/trade_tick_size`) renvoie une valeur déjà convertie en devise du compte. Pour les indices listés dans `_indices_point_val`, un override hardcodé prenait le dessus. |
| Verdict pour les 3 cas concrets ? | **USDCAD GÉRÉ** (forex, voie native MT5). **UK100 NON GÉRÉ avant TÂCHE 0** (override 1.0 USD/pt vs réel 1.36). **GER40 NON GÉRÉ avant TÂCHE 0** (override 1.0 USD/pt vs réel 1.17). **Après TÂCHE 0 : tous les 3 gérés** via voie native MT5. |

**Liste finale des symboles ajoutés à `enabled_symbols`** : 6 candidats (DJ30, UK100, GBPUSD, USDCAD, GER40, XAGUSD) — tous activables grâce à la TÂCHE 0 qui résout le bug d'override pour UK100 et GER40.

---

## 6. Plan de commits proposé (à exécuter après votre validation)

Les commits sont séparés selon votre instruction. Aucun n'est exécuté avant votre feu vert.

```
1) fix(orchestrator): retrait UK100/GER40 du dict point_value override
   Fichier: orchestrator/orchestrator.py (TÂCHE 0)

2) fix(orchestrator): blacklist horaire stricte avec exception XAUUSD nommée
   Fichier: orchestrator/orchestrator.py (TÂCHE 2)

3) feat(profiles): activation shadow mode des 6 candidats
   Fichiers: config/profiles.yaml + sections candidats dans config/overrides.yaml
             (DJ30, UK100, USDCAD, GER40, XAGUSD, GBPUSD)

4) config(bnbusd,solusd): bascule en shadow mode
   Fichier: config/overrides.yaml (auto_execute=false sur BNBUSD/SOLUSD)
```

⚠️ Note : commits 1 et 2 portent sur le **même fichier** `orchestrator.py`. Pour les commits atomiques séparés, j'utiliserai `git add -p` interactif pour stager les hunks séparément. Si vous préférez un commit unique pour les deux (`fix(orchestrator): point_value override + blacklist stricte`), dites-le.

Fichiers prêts à committer : `orchestrator/orchestrator.py`, `config/overrides.yaml`, `config/profiles.yaml`. Aucun autre fichier de code/données touché.

---

## 7. Points à valider avant commit

1. **Position du `BLACKLIST_OVERRIDE_WHITELIST`** : la constante est insérée entre deux blocs d'imports (après `from utils.health import start_health_server`, avant `from agents.whale_agent import WhaleAgent`). Python l'accepte mais c'est non-canonique. Si vous préférez, je peux la déplacer après le dernier import.

2. **`BNBUSD`/`SOLUSD` dans `profiles.yaml`** : leurs profils ont toujours `auto_execute: true` car le brief ne demandait pas de les modifier dans `profiles.yaml`. La désactivation marche via `overrides.yaml` (override de merge). À votre appréciation s'il faut aligner.

3. **`risk_per_trade: 0.005` choisi pour les 6 candidats** : valeur conservatrice (0,5 %) par défaut. Le brief ne spécifiait pas de valeur — j'ai pris une valeur sécuritaire pour le shadow. À ajuster si besoin.

4. **Tag `pre-fix-pointvalue-2026-04-30` créé en plus de `pre-phase2-2026-04-30`** : les deux pointent sur le même HEAD. Vous pouvez en supprimer un si redondant (`git tag -d pre-fix-pointvalue-2026-04-30`).

---

**Fin du rapport. En attente de votre validation pour les commits.**
